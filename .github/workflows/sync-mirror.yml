name: Sync Mirror (Innervycs)

on:
  push:
    branches: [runpod-implementation]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish-mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: runpod-implementation
          fetch-depth: 1

      - name: Publish to Innervycs mirror
        env:
          MIRROR_GITHUB_TOKEN: ${{ secrets.MIRROR_SYNC_PAT }}
          GIT_AUTHOR_NAME: Innervycs Mirror Bot
          GIT_AUTHOR_EMAIL: info@innervycs.com
          GIT_COMMITTER_NAME: Innervycs Mirror Bot
          GIT_COMMITTER_EMAIL: info@innervycs.com
        run: |
          chmod +x scripts/publish-mirror.sh
          ./scripts/publish-mirror.sh
